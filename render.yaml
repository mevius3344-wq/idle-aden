services:
  - type: web
    name: idle-aden
    runtime: node
    plan: free
    branch: main
    autoDeployTrigger: commit
    buildCommand: npm install --omit=dev
    startCommand: node _serve.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
